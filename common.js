const firstNumber = +prompt('Enter the first number to add?');
const secondNumber = +prompt('Enter the second number to add?');

const add = firstNumber + secondNumber;
const sub = firstNumber - secondNumber;
const mult = firstNumber * secondNumber;
const div = firstNumber / secondNumber;


alert(`2 + 2 = ${add}
     2 - 2 = ${sub}
     2 * 2 = ${mult}
     2 / 2 = ${div}`
)

